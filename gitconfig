# This is Git's per-user configuration file.
[user]
    name = Kája Lišková
    email = developer@0xADD1E.me
[alias]
    lg = log --pretty=oneline --graph --abbrev-commit --branches
    fetchall = fetch --prune --tags
[pull]
    rebase = true
[core]
    autocrlf = input
    excludesFile = ~/.gitignore
[init]
    defaultBranch = trunk
[http]
    cookiefile = /Users/kaja/.gitcookies
[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
