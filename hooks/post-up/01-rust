#!/bin/bash
if ! hash cargo 2> /dev/null; then
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
    source ~/.cargo/env
fi
cargo install colorlyne cargo-update cargo-bloat cargo-flamegraph evcxr_jupyter sccache systemfd vape
cargo install-update -a
